# ManiSkill — Skill Run History

Generated by `benchmark-generator` at **2026-05-02T06:50:00+00:00** (rerun on branch `nautilus-rerun-2026-05-01`).

This file is a snapshot of what the skill did in this run — probe results, classification evidence, generated artifacts, smoke-test outcomes, final report. It is regenerated on every re-run.

## Metadata

| Field | Value |
|-------|-------|
| Target repo | `ManiSkill` (https://github.com/YufengJin/ManiSkill, fork) |
| Repo commit | `26e75d3f6838c5d11e88799bd8f270aceb6d307f` |
| Branch | `nautilus-rerun-2026-05-01` |
| Upstream skill | `env-generator` (base docker/) |
| Sub-skill | `benchmark-generator` |
| Benchmark category | **IL** (registry tag; dual-track IL+RL physically) |
| Baselines mode | `n/a` (IL track) |
| Container | `maniskill-headless` |
| Image | `maniskill:local` (verified: `yufengjin/maniskill:latest`, image_id `77730d41e436`, 18.7GB) |

## Classifier evidence

**Main criterion** (`env.step()` returns a reward / `compute_reward` exists): **PASS** — confirmed via L2 smoke; reward is finite float (e.g. `0.0359` on `PickCube-v1`). `compute_reward` defined per task under `mani_skill/envs/tasks/`.

**Auxiliary RL-readiness signals** (8-signal rubric — full RL profile present):

| # | Signal | Result |
|---|--------|--------|
| 1 | `gym.register` (gymnasium-compatible registry) | PASS |
| 2 | VecEnv (GPU-batched via `num_envs=N`) | PASS |
| 3 | `max_episode_steps` per task | PASS |
| 4 | `gym.spaces.Box`/`Dict` action+obs | PASS |
| 5 | README / docs RL keywords (PPO, SAC, etc.) | PASS |
| 6 | Dense reward (multi-term shaped) | PASS |
| 7 | Tuned RL baselines (PPO/SAC/TD3) under `examples/baselines/` | PASS |
| 8 | In-tree `algorithms/` directory | FAIL (baselines live under `examples/baselines/` instead) |

**Aux score**: 7 / 8 → category **dual IL+RL**; resolved to `IL` per registry tag (`category: il` in `mcp/nautilus/data/benchmarks.yaml`) and per the existing skill-generated IL harness (`scripts/run_demo.py`, `scripts/run_eval.py`, `tests/test_random_policy_server.py`). Threshold for RL-only graduation is met but the verified registry chooses IL track for canonical use.

## Probe summary

| Flag | Value | Evidence |
|------|-------|----------|
| `PKG_MGR` | `uv` | env-generator probe |
| `CUDA_EXT_BUILD` | `false` | no in-tree CUDA extensions; SAPIEN ships precompiled `.so` |
| `NEEDS_VULKAN` | `true` | quirk `needs_vulkan_icd` resolved by env-generator (NVIDIA ICD JSON copied) |
| `THIRD_PARTY_SIM` | `SAPIEN + PhysX 5` | imported via `mani_skill.envs` |
| `CONFIG_INIT` | none required | `gym.make` is the entry point |
| `ASSET_BOOTSTRAP` | manual | `python -m mani_skill.utils.download_demo --task <id>` |
| `ACTION_DIM` (IL) | `8` | env `action_space.shape[0]` on `PickCube-v1`; baked into `tests/test_random_policy_server.py` `ACTION_DIM=8` |
| `SMOKE_ENV_ID` | `PickCube-v1` | smallest headless-safe env with finite reward |
| Quirks resolved | `needs_render_libs, needs_setuptools_pin, needs_vulkan_icd` | env-generator output |

## Generated / edited files

| Path | Action | Purpose |
|------|--------|---------|
| `scripts/run_demo.py` | edited (port 8000→8765, `__meta__` envelope injected at init + step) | IL single-episode demo client |
| `scripts/run_eval.py` | edited (port 8000→8765, `__meta__` envelope injected at init + step) | IL multi-episode eval client |
| `tests/test_random_policy_server.py` | edited (default port 8000→8765) | random-action WS server (smoke companion) |
| `docker/Dockerfile` | unchanged this run (`policy-websocket` already installed by env-generator) | base image |
| `.nautilus/benchmark-spec.json` | written | obs/action spec (gripper_convention manually completed) |
| `~/.claude/skills/benchmark-generator/registry/maniskill.json` | written by capture_spec | local registry copy |
| `benchmark.md` | regenerated | benchmark guide + spec sentinel block |
| `history.md` | regenerated | this file |
| `install.md` | unchanged (owned by env-generator) | install steps |

## Tests executed

| Tier | Command | Result |
|------|---------|--------|
| L1 (reset + 10 steps) | `python -c "import gymnasium as gym, mani_skill.envs; env=gym.make('PickCube-v1', num_envs=1, render_mode='rgb_array', obs_mode='rgbd'); env.reset(); [env.step(env.action_space.sample()) for _ in range(10)]"` | **PASS** |
| L2 (reward finite) | same as L1 + `assert reward is not None and np.isfinite(reward)` → printed `L2 OK: reward=0.03589196130633354` | **PASS** |
| L3_IL (WS server + run_demo + run_eval) | bring up `tests/test_random_policy_server.py --port 8765`, socket-poll until ready, run `run_demo.py --policy_server_addr localhost:8765 --env_id PickCube-v1 --num_resets 1 --no_save_video`, then `run_eval.py --policy_server_addr localhost:8765 --env_id PickCube-v1 --num_trials 1`, kill server | **PASS** |

### Captured last-5 stdout lines per tier

**L1+L2 stdout (last 5)**:

```
mani_skill OK
L1 OK
L2 OK: reward=0.03589196130633354
```

**L3_IL run_demo stdout (last 5)**:

```
Server metadata: {'policy_name': 'RandomPolicy(ManiSkill)', 'action_dim': 8}
--- Reset 1/1 ---
Task: Pick the red cube and place it on the green sphere.
  t=0: infer 0.002s
  Episode 0: FAILURE (length=40)
```

**L3_IL run_eval stdout (last 5)**:

```
FINAL RESULTS
Policy:           randomPolicy
Task:             PickCube-v1
Success rate:     0.0000 (0%)
Avg ep length:    40.0
```

(Failure / 0% is expected — RandomPolicy never solves the task; the goal of L3 is wiring validation, not policy quality.)

## Capabilities probed in the container

| Check | Expected | Result |
|-------|----------|--------|
| `nvidia-smi` | GPU visible | PASS — NVIDIA GeForce RTX 4090, driver 570.211.01, CUDA 12.8 |
| `python -c "import torch; print(torch.cuda.is_available())"` | `True` | PASS — `True 2.8.0+cu128` |
| `python -c "import mani_skill; print(mani_skill.__file__)"` | resolves under `/workspace/maniskill/` | PASS — `/workspace/maniskill/mani_skill/__init__.py` (editable mount) |
| `vulkaninfo --summary` | driver + GPU listed | PASS — `NVIDIA GeForce RTX 4090` PHYSICAL_DEVICE_TYPE_DISCRETE_GPU |
| `python -c "from policy_websocket import WebsocketClientPolicy, WebsocketPolicyServer"` | both importable | PASS |

## Diagnostics applied

None on the smoke-test critical path. Three cosmetic/contract patches applied to existing skill artifacts (idempotent on rerun):

| Path | Change summary |
|------|----------------|
| `scripts/run_demo.py` | port default 8000→8765 (matches `policy_websocket` lib default); injected `__meta__` envelope on init + every step payload |
| `scripts/run_eval.py` | port default 8000→8765; injected `__meta__` envelope on init + every step payload |
| `tests/test_random_policy_server.py` | port default 8000→8765 |

## Final report

ManiSkill rerun completed successfully. The IL evaluation harness (random policy server + WebSocket-based run_demo/run_eval) was already in place from a prior verified run; this rerun:

1. Confirmed env-generator's container (`maniskill-headless`) is healthy and `mani_skill` imports cleanly.
2. Reaffirmed IL classification (registry tag `il`, dual-track but IL is the canonical evaluation surface).
3. Brought the existing scripts up to current contract (port 8765 default, `__meta__` envelope on every payload).
4. Re-ran L1/L2/L3_IL smoke — all PASS. The handshake confirmed `action_dim=8` server metadata matches what `PickCube-v1` exposes; episodes run end-to-end.
5. Re-captured obs/action spec via `capture_spec.py` with `PickCube-v1` ↔ `PushCube-v1` cross-task verification (identical action_spec + filtered obs_spec). Persisted to both `.nautilus/benchmark-spec.json` and the local registry. Manually filled `gripper_convention` from the verified historical entry (`{open:+1, close:-1, binary:true, index:7}`).
6. Regenerated `benchmark.md` + `history.md`. `install.md` left intact (owned by env-generator).

### Known limitations

- env-generator tier2 reported partial (9/18) imports; the 9 failures are in `examples/baselines/*` requiring optional deps (diffusers, hydra, jax, wandb, etc.) — not relevant to the core IL/RL eval path. Core `mani_skill` module imports cleanly.
- `gripper_convention` cannot be auto-introspected by `capture_spec.py` (no controller-side metadata exposes it). Manually filled here from the prior verified spec.
- `benchmark.md` lists representative tasks; the full registered-env count is dynamic — use `python scripts/run_demo.py --list_tasks` for the live list.
- L3_IL random policy never succeeds (expected); replace with a real WebSocket policy server to measure success rate.

### Next actions the user can take

```bash
# Work inside the container
docker exec -it maniskill-headless bash

# Stop / restart
docker compose -f docker/docker-compose.headless.yaml down
docker compose -f docker/docker-compose.headless.yaml up -d

# Re-run the IL smoke loop
docker exec -d maniskill-headless python tests/test_random_policy_server.py --port 8765
docker exec maniskill-headless python scripts/run_demo.py \
    --policy_server_addr localhost:8765 --env_id PickCube-v1 --num_resets 1
docker exec maniskill-headless python scripts/run_eval.py \
    --policy_server_addr localhost:8765 --env_id PickCube-v1 --num_trials 10

# Train an RL baseline for real (native ManiSkill PPO)
docker exec maniskill-headless python examples/baselines/ppo/ppo.py \
    --env_id="PickCube-v1" --num_envs=2048 --total_timesteps=10_000_000

# Download IL demonstrations
docker exec maniskill-headless python -m mani_skill.utils.download_demo \
    --task PickCube-v1
```

---

### How this file is regenerated

Re-run `benchmark-generator` against the repo (typically dispatched automatically by `env-generator` when classification is `benchmark`). The skill overwrites both `benchmark.md` and `history.md`. To diff before overwriting, rename the old copies first.
